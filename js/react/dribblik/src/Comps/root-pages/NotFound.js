import React from "react";

function NotFound({themeb}) {
    return (
        <p className="mt" style={{color: themeb.color}}>Sorry, this page is not found (404)</p>
    )
}

export default NotFound;